# Blackhole Voter Configuration
# Copy this to .env and fill in your values
# NEVER commit .env to git!

# Main wallet address (for voting history, transaction analysis, etc.)
# BLACKHOLE_WALLET_ADDRESS=0x...

# Main wallet private key (for production voting)
# BLACKHOLE_VOTER_PRIVATE_KEY=0x...

# Test wallet private key (for testing - recommended)
# BLACKHOLE_TEST_WALLET_KEY=0x...

# Usage:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env and add your wallet address and keys
# 3. Ensure .env is in .gitignore (already configured)
# 4. Scripts will automatically load from .env if python-dotenv is installed
